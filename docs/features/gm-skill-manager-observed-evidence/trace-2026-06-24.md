# Trace Report — Observed Evidence Deepening

## Goal

Implement the accepted Runtime Facts → Observed Evidence → Identity Resolution design and replace Unified Inventory schema `1.0` with `2.0`.

## Complexity

L2

## Files changed

- `scripts/observed_evidence.py`: owns schema `2.0`, Evidence Subject, Validity, Identity, Provenance, Findings, defaults, and Runtime-specific interpretation.
- `scripts/adapters/`: emits Runtime Facts through one orchestration seam; adds unmanaged ZCode detection.
- `scripts/inventory.py`: orchestrates adapters without Runtime-specific calls or ZCode special cases.
- `scripts/inventory_model.py`: creates entities only from valid installation evidence and derives cross-record conflicts.
- `scripts/views.py`, `scripts/doctor.py`: consume schema `2.0` diagnostics and runtime detection.
- `tests/`: verifies Runtime Facts, normalization, stable Evidence Identity, invalid isolation, npx multi-Exposure, ZCode detection, views, and read-only behavior.
- `SKILL.md`, references, glossary, ADRs, and design docs: align instructions with the implemented seam.

## Verification evidence

- Command: `python3 -m unittest discover plugins/gm-skill-manager/skills/gm-skill-manager/tests -v`
- Result: pass, 25 tests.
- Command: `python3 -m compileall -q .../scripts .../tests`
- Result: pass.
- Command: real-machine `inventory.py --json`
- Result: schema `2.0`; 102 evidence records; 16 npx records with multiple Runtime Exposures; ZCode has zero packages/installations and appears as unmanaged runtime detection.

## Not verified

- No mutation Action was executed; Native Installer ownership is unchanged.

## Remaining risks

- Upstream native payload variants not represented by fixtures will become Fact Collection Findings or invalid evidence and may require a new Runtime-specific interpreter branch.
- Schema `2.0` intentionally replaces `1.0`; external consumers must update atomically.

## Rollback

Revert the Observed Evidence module, Runtime Facts adapters, schema `2.0` inventory changes, tests, and aligned documentation as one change.
